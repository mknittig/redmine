<h2><%= l(:label_board) %></h2>

<% labelled_tabular_form_for :board, @board, :url => {:action => 'update', :id => @board}, :html => { :method => :put } do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_save) %>
<% end %>
